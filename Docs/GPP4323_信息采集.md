# GPP-4323 四通道电源 — 集成信息采集表

> 请在 `你的值/指令` 列填入实测数据。

---

## 一、基本连接参数

| 项目  | 你的值   |
| --- | ----- |
| 串口号 | COM11 |
| 波特率 | 9600  |
| 数据位 | 8     |
| 校验位 | None  |
| 停止位 | 1     |

---

## 二、通道分配

| 用途      | 通道号 | 电压  |
| ------- |:---:|:---:|
| RX 接收供电 | CH1 | 12V |
| TX 发射供电 | CH2 | 24V |

---

## 三、SCPI 指令（一条指令直接操作指定通道，无需切换）

> 请填入你实测成功的指令。`{ch}` 表示通道号，请用实际数字替换后填入。

### 3.1 输出开关

| 功能       | 指令示例（选一个填入正确的）                                 | 你的实测指令     |
| -------- | ---------------------------------------------- | ---------- |
| 开 CH1 输出 | `:OUTP1 1` 或 `:CH1:OUTP 1` 或 `:SOUR1:OUTP ON`  | :OUTP1 On  |
| 关 CH1 输出 | `:OUTP1 0` 或 `:CH1:OUTP 0` 或 `:SOUR1:OUTP OFF` | :OUTP1 Off |
| 开 CH2 输出 | (另一个通道)                                        | :OUTP2 On  |
| 关 CH2输出  | (另一个通道)                                        | :OUTP2 Off |

### 3.2 测量

| 功能       | 指令示例                               | 你的实测指令       |
| -------- | ---------------------------------- | ------------ |
| 读 CH1 电压 | `:MEAS1:VOLT?` 或 `:MEAS:VOLT? CH1` | :MEAS1:VOLT? |
| 读 CH1 电流 | `:MEAS1:CURR?` 或 `:MEAS:CURR? CH1` | :MEAS1:CURR? |
| 读 CH2 电压 | (另一个通道)                            | :MEAS2:VOLT? |
| 读 CH2 电流 | (另一个通道)                            | :MEAS2:CURR? |

### 3.3 设置

| 功能       | 指令示例                               | 你的实测指令   |
| -------- | ---------------------------------- | -------- |
| 设 CH1 电压 | `:SOUR1:VOLT 12.0` 或 `:VOLT1 12.0` | VOLT1 12 |
| 设 CH1 电流 | `:SOUR1:CURR 1.0` 或 `:CURR1 1.0`   | CURR1 1  |
| 设 CH2 电压 | (另一个通道)                            | VOLT2 12 |
| 设 CH2 电流 | (另一个通道)                            | CURR2 1  |

### 3.4 基础查询

| 功能       | 你的实测指令 |
| -------- | ------ |
| 查询仪器 IDN | 标准SCPI |
| 查询错误队列   | 标准SCPI |

人类备注:标准SCPI命令是通用的,无需备注

---

## 四、补充

> 指令结尾符是 `\n` 还是 `\r\n`？回复结尾是？ \n
> 有没有其他需要注意的（如 DTR/RTS 控制等）？
